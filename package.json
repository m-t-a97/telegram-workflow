{
  "name": "@telegram-workflow/main",
  "version": "0.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "clean": "lerna clean -y",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "start:dev": "lerna run start:dev",
    "test": "lerna run test:unit"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
