{
  "name": "@telegram-workflow/main",
  "version": "0.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "clean": "lerna clean -y",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "start:dev": "lerna run start:dev",
    "start:prod": "lerna run start:prod",
    "run:prod": "npm run bootstrap && npm run build && npm run start:prod",
    "test": "lerna run test:unit"
  }
}
